{
  "name": "gdi-intermediate-python",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "start": "reveal-md presentation.md -w",
    "deploy": "npm run build && surge -p dist/ -d gdi-intermediate-python.surge.sh",
    "build": "rimraf dist/ && reveal-md presentation.md --static dist && mkdir -p dist/images/ && cp -r images/ dist/images && cp -r css/ dist/css && cp -r js/ dist/js"
  },
  "author": "Steven Loria",
  "license": "MIT",
  "dependencies": {
    "reveal-md": "^1.1.3",
    "rimraf": "^2.6.2",
    "surge": "^0.19.0"
  }
}
